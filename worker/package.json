{
  "name": "aerekos-cloud-worker",
  "version": "1.0.0",
  "description": "Worker node for aerekos-cloud",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest --testPathPattern=tests",
    "test:watch": "jest --watch --testPathPattern=tests",
    "test:coverage": "jest --coverage --testPathPattern=tests",
    "test:integration": "jest --testPathPattern=tests/integration",
    "test:unit": "jest --testPathPattern=tests/unit"
  },
  "keywords": [
    "cloud",
    "worker",
    "orchestration"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "dockerode": "^4.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "js-yaml": "^4.1.0",
    "socket.io-client": "^4.8.1",
    "systeminformation": "^5.21.20"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
